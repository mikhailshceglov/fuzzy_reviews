## Оффлайн: построение авто-словаря нейросетью

**Шаг A0. Выбор модели**
* Берём готовую русскую модель сентимента по предложениям (нейросеть, не мы обучали).

**Шаг A1. Корпус**
* Собираем большую кучу отзывов «как есть» из разных доменов (товары, кафе, игры и т.д.).
**Шаг A2. Прогон через модель**

Для каждого предложения в корпусе:

1. Лемматизируем текст.
2. Гоним предложение через модель → получаем `sentiment_score ∈ [−1; 1]`.

**Шаг A3. Оценка слов**
Для каждой леммы `w`:

* собираем все предложения, где она встречалась;
* считаем:

  * средний `polarity(w)` = средний `sentiment_score` этих предложений;
  * частоту появления `freq(w)`.

Фильтруем:

* оставляем только леммы с `freq(w) ≥ N_min` (чтобы не было шума от редких слов);
* получаем авто-лексикон:
  `w → polarity(w) ∈ [−1; 1]`.

**Шаг A4. Быстрая проверка качества**
* Смотрим топ-слова по |polarity| и около 0;
* Подчищаем явные косяки руками (немного).

===> Результат оффлайн-этапа: **большой словарь лемм с полярностью**, полученный автоматически и более-менее доменно-независимый.

---

## Онлайн: обработка конкретного отзыва + fuzzy

Теперь пайплайн для **одного** отзыва.

### 1. Предобработка текста (NLP)

**Вход:** текст отзыва.

1. Нормализация: нижний регистр, чистим мусор.

2. Токенизация + лемматизация.

3. Для каждой леммы:

   * смотрим, есть ли она в авто-словаре `polarity(w)`;
   * отмечаем усилители/хеджеры/«не» (по небольшому ручному списку):

     * усилители: `очень, безумно, ужасно (в смысле "очень"), супер`…
     * смягчители/неопределённость: `немного, слегка, скорее, в целом, как будто, наверное`…
     * отрицание: `не`.

4. Применяем модификаторы:

   * `очень хороший`: увеличиваем |polarity(«хороший»)|;
   * `немного разочарован`: уменьшаем |polarity(«разочаровать»)|;
   * `не плохой`: сдвигаем `polarity(«плохой»)` в сторону нуля/слегка в плюс.

👉 На выходе шага: список оценочных вкладов (чисел) по словам.

---

### 2. Расчёт «чётких» признаков (crisp features)

Из вкладов считаем:

1. Суммарный позитив:
   [
   pos = \sum_{polarity(w) > 0} polarity(w)
   ]

2. Суммарный негатив:
   [
   neg = \sum_{polarity(w) < 0} -polarity(w)
   ]

3. Главное число:
   [
   score_crisp = \frac{pos - neg}{pos + neg + 1}
   ]

4. Дополнительные признаки:

   * `emotion_intensity_crisp` = сумма |polarity(w)| или среднее по оценочным словам;
   * `hedges_count` = сколько хеджеров/смягчителей в тексте;
   * `coverage` = доля слов отзыва, для которых нашёлся `polarity(w)`
     (мера «насколько наш словарь понимает этот текст»).

👉 Это всё обычные числа: тональность, интенсивность, неопределённость языка, покрытие словарём.

---

### 3. Фаззификация (начинается нечеткая логика)

Вводим **лингвистические переменные**:

1. **Общая_тональность** (по `score_crisp`, шкала [−1; 1])
   Термы:

   * «сильно_отрицательная»,
   * «умеренно_отрицательная»,
   * «нейтральная»,
   * «умеренно_положительная»,
   * «сильно_положительная».

2. **Интенсивность_эмоций** (по `emotion_intensity_crisp`)
   Термы: «низкая», «средняя», «высокая».

3. **Лингвистическая_неопределённость** (по `hedges_count`)
   Термы: «почти_нет», «умеренная», «сильная».

4. **Надёжность_лексикона** (по `coverage`)
   Термы: «низкая», «средняя», «высокая».

Для каждой переменной задаём **функции принадлежности** (треугольники/трапеции) и считаем:

* μ_сильно_положительная(score_crisp), μ_нейтральная(score_crisp)…
* μ_интенсивность_высокая(emotion_intensity_crisp)…
* μ_неопределённость_сильная(hedges_count)…
* μ_надёжность_низкая(coverage)…

👉 Теперь у нас «мягкие» высказывания: «скорее положительный», «эмоции высокие», «формулировки сильно неопределённые», «словарь мало что понял».

---

### 4. Нечеткий вывод по правилам

Определяем выходные лингвистические переменные, например:

* **Итоговая_тональность** (тот же диапазон [−1; 1], с термами, как у `Общая_тональность`);
* **Доверие_к_оценке** («низкое, среднее, высокое»).

Пишем правила, например:

1. ЕСЛИ `Общая_тональность` = «умеренно_положительная»
   И `Интенсивность_эмоций` = «низкая»
   И `Лингвистическая_неопределённость` = «почти_нет»
   И `Надёжность_лексикона` = «высокая»
   ТО `Итоговая_тональность` = «слабоположительная»
   И `Доверие_к_оценке` = «высокое».

2. ЕСЛИ `Общая_тональность` = «нейтральная»
   И `Лингвистическая_неопределённость` = «сильная»
   ТО `Итоговая_тональность` = «нейтральная»
   И `Доверие_к_оценке` = «низкое».

3. ЕСЛИ `Общая_тональность` = «сильно_отрицательная»
   И `Интенсивность_эмоций` = «высокая»
   И `Надёжность_лексикона` = «средняя»
   ТО `Итоговая_тональность` = «сильно_отрицательная»
   И `Доверие_к_оценке` = «среднее».

4. ЕСЛИ `pos` ≈ `neg` (можно через отдельную переменную «баланс_оценок»)
   И `Интенсивность_эмоций` = «высокая»
   ТО `Итоговая_тональность` = «контрастная» (есть и сильные плюсы, и минусы).

Любой стандартный алгоритм нечеткого вывода (Мамдани, Сугено) агрегирует эти правила и выдаёт:

* нечеткое множество для **Итоговая_тональность**;
* нечеткое множество для **Доверие_к_оценке**.

---

### 5. Дефаззификация и итог

1. Дефаззифицируем **Итоговую_тональность** → `final_score ∈ [−1; 1]` или [0; 100].
2. Дефаззифицируем **Доверие_к_оценке** → `reliability ∈ [0; 1]`.
3. Храним также **лингвистические метки**:

   * «скорее положительный отзыв, эмоции умеренные, формулировки мало неопределённые, надёжность оценки высокая»;
   * «нейтральный, много размытых выражений, доверие низкое» и т.п.

---

Итого:

* авто-словарь от нейросети даёт **общий, доменно независимый источник полярностей**;
* на каждом отзыве мы используем этот словарь как «сенсор» → числа;
* нечеткая логика — сердцевина интерпретации: она учитывает **тональность, эмоции, лингвистическую неопределённость и надёжность словаря** и даёт человеку понятную, «мягкую» оценку.
